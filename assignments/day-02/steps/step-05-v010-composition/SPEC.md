# Step 05 spec — v0.10 source composition

## Outcome

Replace the rejected circular-node prototype with a Day 2 introduction page composed from the latest NEXT SAVE product and design resources while preserving the already-tested demo-entry behavior.

## Source inputs

- `assignments/day-02/resources/SOURCE-MANIFEST.json`
- Design v0.10 Hero, grid wash, cool tokens, copy deck, and latest render captures.
- Idea R8/current soft-core evidence and the explicit fixture-only course boundary.

## Owned files

- `app/page.tsx`, `app/globals.css`, `app/layout.tsx`
- `components/demo-experience.tsx`, `components/atlas-teaser.tsx`
- `lib/demo.ts`, `lib/demo.test.ts`
- This step packet and Day 2 progress/evidence rollups.

## TDD slices

1. Red: require the imported v0.10 Hero copy contract.
2. Red: require the teaser tile map to be one four-directionally connected continent with only declared biome codes.
3. Green: add source-locked copy/data and implement the responsive page composition.
4. Refactor: remove circular nodes, warm atlas chrome, and duplicated disclosure while preserving validation and two fixture profiles.

## Acceptance checks

- Hero wording and structural motif match the imported v0.10 source.
- The visual teaser is pixel-based, cool-only, and one connected continent; there are no circular taste nodes.
- URL validation, exactly two profile fixtures, profile change live announcement, and fixture disclosure remain intact.
- No runtime Steam/CDN/font request is introduced.
- Unit tests, production build, browser flow, 360 px layout, keyboard focus, and screenshot review pass.
