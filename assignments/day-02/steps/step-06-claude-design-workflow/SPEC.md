# Step 06 spec: Claude Design workflow and proposal

## Outcome

Document the first-party Claude Design workflow and use it to create a bounded Day 2 visual proposal before changing the shared application files.

## Owned files

- `docs/claude-design/**`
- `assignments/day-02/steps/step-06-claude-design-workflow/**`

Application files and shared Day rollups are excluded until the generated proposal is reviewed and a separate implementation slice is defined.

## Evidence method

This is a research/documentation step, so it uses direct first-party UI observations rather than artificial unit tests.

## Acceptance checks

- Product controls and current Beta limitations are documented.
- Local attachment, GitHub connection, sharing, public publishing, external export, deletion, Git publication, and LMS submission gates are explicit.
- The NEXT SAVE run records design system, template, source mode, model, project URL, prompt contract, and excluded data.
- No repository source, secrets, personal Steam data, or real profile URL is uploaded.
- Generated output is reviewed before any shared-file implementation begins.
