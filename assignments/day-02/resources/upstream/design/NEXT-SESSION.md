# 003 · 다음 세션 핸드오프 (v0.9 → 아틀라스 A/B 하이브리드 스펙 라운드)

> 🟢 **현행 = v0.9 (2026-07-20).** 두 축으로 전환됨:
> 1. **아키텍처 피벗:** 랩은 이제 디자인을 직접 코딩하지 않는다 → **스펙 생산**만. 시각 생성은 **Claude Design vs Pencil A/B 아웃소싱**. 레퍼런스는 컴포넌트 단위 수집.
> 2. **아틀라스 확정:** 흩어진 군도 → **이어진 대륙** + **A/B 하이브리드**(데이터가 형태, 픽셀 모티프가 장르 인식). 레퍼런스 확장(§C) **완료**.
> 👉 **현행 계획서 = `atlas-plan.md`(도메인 분해 + 조사 방안 + 진행 옵션 A~E).** 결정 근거 = `direction.md` 리비전 **v0.9**. 아래 §A~C(구 v0.8 우선순위)는 **재편됨** — 다음 블록 참조.
> 현행 산출물: `copy.ts`(S0~S10 해요체 스파인) · `direction.md`(v0.9) · `atlas-plan.md`(신규 계획서). 렌더 = 라이브(아래 뷰잉).

## ⚡ v0.9 재편 — 다음 작업 (구 §A~C 대체)
- **[임계경로] 옵션 A** = 12장르 **모티프 어휘(biome 사전) + 고정 인접 그래프 + 색·고도 램프** 스펙 확정 → 이게 생성기 입력. `atlas-plan.md §3` 참조.
- **[병렬] 옵션 D** = 배경 좌표그리드 워시 + Hero 대륙 티저(저리스크, v0.7 최대 지적 완화).
- **[A 확정 후] 옵션 C** = Claude Design vs Pencil A/B 첫 시도(Pencil `get_guidelines` 선독).
- **그 다음** = `design-system.md` seed(게이트 해제됨, A 산출물+오너 판정 후) · 옵션 E(상호작용·모바일, 형태 확정 후).
- **격하됨:** 구 §B "atlas.tsx 625줄 hand-split + 해요체 포팅" → 코드 수술 대신 **스펙 규율**로 생성기에 전달(아웃소싱 피벗). 기존 코드는 참조 프로토타입.
- **열린 판정(오너):** `atlas-plan.md` 하단 6건 — 특히 ①형태 레버 선택 ②레퍼런스 주축·gameweb 채택.

### 🔵 최신 열린 스레드 (2026-07-20 세션 말미 — 여기서 멈춤)
> 이 세션 후반은 아틀라스 표현을 오너와 대화로 좁힘. 시각 근거 = Artifact 6종(`atlas-explorations/`에 소스 보존, URL은 `direction.md` v0.9). 아래 **#5는 판정됨**(맵=활성 기본값·나머지는 스왑 후보군). 남은 **#4 다이얼은 UX-게이트로 이월**.
- **[교정, direction.md v0.9 #4] 심볼-퉁 기각 → "맵 자체가 그 장르"(지형-네이티브).** A-1 사전 "오브젝트형" 제거 후보. 해소 = 한 매질 유지 시 대륙 양립. **다이얼 위치(★지형-네이티브 vs 더 정체성) 확정 대기.**
- **[판정 2026-07-20, direction.md v0.9 #5] 표현 방식 = 맵(대륙) 활성 기본값.** 엠블럼 최종 기각. "각 장르 전면 이미지화" 등 나머지 형태(문장/포스터/성좌) = **스왑 가능 후보군**으로 보존(맵=확정 아님, "언제든 바꿀 수 있게"). 스왑 게이트 = **UI/UX 전체 관점 재검토**(취향만으로 안 잠금 — 오너). 채택 이유 = 결정지원 기둥(땅 클릭→추천) 유지.
- 남은 게이트 = **#4 다이얼**(지형-네이티브↔정체성)도 같은 UX-게이트 아래 열림. A-1 스펙(biome 사전)은 **맵 기본값 위에서 착수 가능**하되 이미지화 강도는 파라미터로 열어둠 → design-system.md seed → 생성기 A/B.

## 0. 뷰잉 (먼저)
- 이 체크아웃에 `node_modules` 있음(이번 세션 확인). 없으면 `npm install`.
- dev 서버(이번 세션 기동, 백그라운드 계속 떠 있음): `node node_modules/next/dist/bin/next dev -H 0.0.0.0 -p 3111` (`-H 0.0.0.0` = Tailscale 뷰잉 필수).
- 사용자는 원격 SSH → **Tailscale**: `http://100.105.231.73:3111/e/003-next-save-landing`.
- Next 16.2.10 (Turbopack). ⚠ AGENTS.md: 이 Next는 브레이킹 있음 — 코드 짜기 전 `node_modules/next/dist/docs/` 참조.

## 이번 세션(2026-07-14) 확정 — 되돌리지 말 것
- **카피 보이스 = 해요체 담백 (오너 통과).** 백본 = 해요체 + 1인칭 "내/나의" + 정직어휘("사놓고 안 한 게임/유기"). 정직 메타(데모배너·trustNote) = 합니다체. **슬랭·음슴체는 리뷰(S8)에만 격리**, 비속어 = **Level 1**(강욕 시발/존나만 제거, 갓겜·순삭·유기 유지). 근거·기각 전문 = `direction.md` 리비전 v0.8.
- 히어로 "쌓아둔 게임…정해준다" → **"사놓고 안 한 게임 수백 개, 오늘 밤 켤 한 판까지."**
- 기존 통과분(누적): 지도 실측 정합(taste #1), 데모 정직성(taste #3), 쿨 단일 + 웜은 커버/별점만(taste #2), 카드 일변도 경계(taste #4), 장르 상징 스프라이트, 측량 결과물 제거.

## 오너 렌더 피드백 (2026-07-14, 신규 — 미해결)
1. **픽셀 스프라이트 뭉개짐(판독 불가):** **액션(번개)·로그라이크(주사위)·FPS(권총)·대전격투(맞붙은 둘)·퍼즐(조각)** — 이전 3종(퍼즐·권총·맞붙은둘)에서 **액션·로그라이크까지 확대**. 목표 수준 = 레이싱 깃발·검·열쇠·집·해골. 수정 위치 = `atlas.tsx` line 103 `SPRITES` 비트맵만(엔진 불변).
2. **맵이 좁다(?):** 아틀라스 지도 영역이 좁아 상징이 뭉개지는 것과 연동. 캔버스/셀 스케일 재검토(스프라이트 재도트와 함께 판단).
3. **어체 = 통과.** 해요체 전환 OK. (S5 "이게 나네요" 귀여움 수위는 아직 오너 취향 잔여, 급하지 않음.)

## 다음 작업 — 오너 방향 전환 (우선순위 재편)
> 오너 지시: "세부 조정할 부분을 빼고 우리 design.md 정립하고 컴포넌트 관리를 해야겠네. 레퍼런스 좀 더 보고 판단해봐야겠다." → **미시 폴리시(스프라이트·맵·티저 등) 보류, 기반부터.**

### A. [최우선·기반] 우리 `design-system.md` 정립
- **이름 확정(오너 2026-07-14):** 파일명 = **`design-system.md`**. (랩에서 `design.md`는 레퍼런스 역공학 산출물 이름이라 충돌 회피.)
- **문제:** 003 자체 디자인 시스템 스펙 **부재**. 색·타입·레이아웃·시그니처·모션이 `direction.md` v0.1~v0.8 **리비전 히스토리에 흩어짐** — 현행 진실을 한눈에 못 봄.
- **할 일:** `direction.md`는 **결정 장부(why·히스토리)**로 두고, **현행 진실 스냅샷**을 canonical `design-system.md`로 분리 정립. 토큰(현행 팔레트 = 쿨 단일 ink+teal, 웜=커버/별점만 / 폰트 = Pretendard 브랜드 / 시그니처 = 픽셀 아틀라스)·타입 역할·레이아웃 개념·모션 규율을 한 파일로.
- ⚠ **레퍼런스 확장(C) 결과를 보고 확정** — 오너가 "레퍼런스 더 보고 판단" 명시. `design-system.md` 조기 확정 금지.

### B. 컴포넌트 관리
- **`atlas.tsx` = 625줄 모놀리스** (지도 엔진 + 12장르 데이터 + 판정 + 모달 + 추천카드 전부). 분할 대상. + 아직 **구버전 레지스터**("너"+한다체) → 해요체 포팅을 이 분할 라운드에 **동시** 처리(스파인만 해요체라 한 화면 말투 갈림 = 검수 최대 잔여 리스크).
- **`landing.css` = 2405줄** — 죽은 레거시 CSS 축적(구 정체성카드/리뷰/티커, retro v0.4 기록). 정리·모듈화.
- page.tsx 스파인은 이미 `components/` 8종 분할됨(Nav·DemoBar·Hero·ProfileStrip·HowItWorks·Community·Footer·Icons) — 이 방식을 atlas에도.

### C. 레퍼런스 확장
- **design.md 확정 전** 레퍼런스 추가 역공학(`/design-deconstruct`). 현재 `references/`: backloggd·stats-fm·ramp·controls·playful-cartoon-folio·violet-teal-saas-landing.
- 목적: 픽셀 아틀라스 시그니처·게임 취향 시각화·컴포넌트 패턴의 새 레퍼런스로 design.md 근거 보강.

### (보류) 세부 조정 — 기반 정비 후 재개
- 스프라이트 재도트(액션·로그라이크·FPS·대전격투·퍼즐) + 맵 스케일 — B/design.md 정리 후.
- 히어로 우측 티저 · 카드 리듬 해체(taste #4) · 배경 무대감 · 모바일 지도 라벨.
- (기능·보류분) F1 `type=url` 회귀 · W3 포커스 가림 — 오너 지시로 계속 보류.
- S5 "이게 나네요" 어체 미세조정.

## 거버넌스 대기 (오너 결정)
- **domain-research 고문 accepted 승격** — 게이트 재료 누적: ①ux-heuristics 첫 감사 hit ②voice-corpus 1회차 hit ③**이번 경쟁사 teardown hit**(v0.8 카피 결정의 직접 근거). 승격 조건 충족 방향 — registry status 갱신 + ADR-0003 게이트 통과 기록 검토.

## 미정 / 대기 (idea 쪽)
- **정식 `/handoff` (idea 원장) 여전히 미착지.** design이 R2 이후 선행(아틀라스 피벗·측량 결과물 제거·카피 해요체)했으므로, 도착 시 원장과 **대조·핑퐁**. 특히 "아키타입 제거"는 idea 스펙 FR-004~005와 어긋날 수 있음.
- **O2** 헤드라인 무게추(정체성 vs 유틸) → idea `/speckit-clarify` 대기.
