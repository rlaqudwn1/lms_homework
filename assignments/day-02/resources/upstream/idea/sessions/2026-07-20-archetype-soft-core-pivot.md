# 세션 로그 — 아키타입 소프트 코어 피벗 (2026-07-20)

> **이 세션의 결정·산출 아카이브(이력 보존).** 라이브 착수 큐는 루트 **`NEXT.md`**로 이관됨 — 다음 세션이 자동 로드하는 건 그쪽이다. 이 파일은 감사 추적용 상세 기록.
>
> ⚠️ **용어:** "handoff"는 **A = idea↔design 원장**(`handoff/next-save.md` + `/handoff` 스킬) 전용. 세션→다음세션 연속성(B)의 라이브 큐 = `NEXT.md`, 이력 = 이 `sessions/` 폴더. (2026-07-20 조사로 확정: `NEXT.md` + CLAUDE.md `@import`.)

## 오늘 결정 (확정)

- **결정 A** — 아키타입을 **넓이×깊이 2×2 하드 격자 → 신호 정렬 소프트 코어 5종**으로 전환.
  - 출력 = 하드 라벨 1개가 아니라 **코어 + 강도% + 보조 태그 + 개인 신호 근거**.
  - 근거 = GPT-5.6 Pro 검토 + UX·심리 서브에이전트 + 도메인 분석 + design 트랙이 **독립적으로 같은 방향**.
  - 이 결정으로 **"4종=표면 / 5종=내부" clarify(2026-07-10)가 역전됨** — 5 페르소나가 표면 코어로 승격. spec FR-004/005·헌법·clarify 기록 갱신 필요(미반영, NEXT).
- **시각화** = 아티팩트 "아키타입 구조 변경 — Before/After" (제안·결정대기 배지). URL은 대화 참조.
- **판단 근거 초안** = `research/signal-sufficiency-sheet.md` §2.5에 작성(초안, 조사 후 확정).

## 오늘 발견 — 판단 근거는 GPT의 1:1보다 복잡

- **경쟁소셜형 = 복합신호** (S4 멀티비중 × 경쟁 장르 태그). S4 단독은 협동(co-op)과 경쟁을 뭉갬. → 사용자 지적, 확정.
- **S3 장르 분포 = 공유 기반(substrate)** — 잡식가 넓이 + 경쟁 태그 + 헌신형 몰린 장르를 동시에 먹임. 신호 직교 아님.
- **헌신형 신호 귀속 불일치** — 시트 §2=S3, GPT=S1. 상관 확인 후 하나로.

---

## 다음 일 (NEXT — 착수 큐)

### ① 판단 근거 = 초안 → 확정 (조사·분석·연구 게이트)
- 초안 위치 = 신호 시트 §2.5. **확정 전 필요:**
  - **§4 프로필 실투입** (공개 Steam 프로필 5~8개) — 신호 분리도·재현성 실측.
  - 헌신형 **S1 vs S3 상관** 확인 → 신호 귀속 확정.
  - 경쟁형 ↔ 협동소셜형 **코어 분리 여부**(5→6) 판정 — 신호가 갈라주는지.
- 게이트 = red-team #1(신호 충분성) 시트 판정과 동일 트랙.

### ② 장르 → 코어 매핑 조사 (서브에이전트)
- **과제:** 12대륙 장르(RPG·어드벤처·전략·퍼즐·액션·레이싱·시뮬·메트로배니아·로그라이크·호러·FPS슈팅·대전격투) 각각이 **어느 코어에 신호를 주는지** 매핑표.
- **핵심:** Steam 장르/태그 taxonomy 접지 → **경쟁 태그 정의**(대전격투·FPS경쟁·MOBA·배틀로얄·히어로슈터 등 PvP 계열)로 경쟁소셜형 복합신호 조작화.
- **산출:** 장르×코어 매핑 표 + 경쟁/협동 태그 분리 기준.

### ③ 아티팩트 주석 반영
- "아키타입 구조 변경" 아티팩트에 **"일부 코어 = 복합 신호(경쟁소셜형 = S4 × 장르)"** + "S3 = 공유 기반" 주석 추가.
- 코어↔신호 정렬표를 "1:1 초안 → 복합 반영" 으로 갱신.

---

## 별도 대기 (더 큰 건 — design 정합)

> A 결정 났으니 이제 열 수 있는 **`/handoff next-save` 라운드**. ②③과 별개.

design이 원장 밖에서 4일·4버전 앞서갔고 idea R7이 미도달. 다음 `/handoff`에서 정합할 것:
1. **[최우선] 아키타입 거취** — design은 v0.7(7-13)에 아키타입 판정을 **화면에서 삭제**(FR-005 상충). A 결정(소프트 코어) = design "측정 방식만 설명"과 화해 가능 → design에 **소프트 코어 복원** 방향 전달.
2. **NEXT PICKS 블록** — idea FR-014(별도 ④) vs design(지도 흡수) 합의.
3. **데모 프로필 2개**(O3) — design은 1개(`@frost_warden`)만. 2번째 데이터셋 요청.
4. **O2′ 회신** — design R8(히어로 무게추) 미회신. 정식 라운드로 회수.
5. **원장 동기화** — design 사본 `idea_handoff_07-08.md`가 R2에서 정지. R4~R7 + 이번 라운드를 `../design`에 반영.
6. **트렌드 게임 섹션** — design 독자 추가(v0.7). 승인 여부 판정.

## 반영 대기 문서 (A 결정의 여파)
- `spec` FR-004/005 · 헌법 · clarify 기록 — "4종=표면/5종=내부" → "소프트 코어 5종" 로 갱신 필요.
- `handoff/next-save.md` — 아틀라스·A결정 미반영(stale).

---

## 추가 결정 (같은 날, 후속 세션) — NEXT.md 도메인 보드 개편

**결정:** 세션 연속성 큐(B)를 평면 큐 → **도메인 보드**로 개편. 할 일이 "착수 큐/별도 대기/반영 대기/병행 대기" 4개 섹션에 흩어져 도메인 소속·의존관계·병렬 가능 여부가 안 보이던 문제 해소(누락 방지 + 병렬 실행).

**도메인(경계 컨텍스트) 4개:**
- **M 아키타입 모델**(코어) — M1 판단근거 게이트 · M2 장르→코어 매핑 · M3 아티팩트 주석 · M4 네이밍(O1 잔여)
- **X Design 정합**(인터페이스) — 구 "별도 대기" 6건 = X1~X6, `/handoff` 라운드 일괄. 원장 stale 해소는 X5에 귀속.
- **G 스펙·거버넌스** — G1 spec FR-004/005(⛓X1 대기) · G2 헌법·clarify(즉시)
- **B 비즈니스** — B1 Pro 가격 손익(독립)

**경계 조건:** 물리 폴더는 불변 — `/handoff` 스킬→`handoff/`, speckit→`specs/`, INDEX 자동생성→`ideas/` 경로 의존이 있어 폴더 이동은 셋 다 파손. 도메인 = NEXT.md 추적 레이어로만 도입.

**도메인 간 의존 명시:** X1→G1, M1→G·X, M4→G·X, M2→M1 (NEXT.md "도메인 간 의존" 섹션).

**보강(같은 세션):** 도메인 보드에 **연구 단계 규약** 추가 — 각 태스크에 `조사→분석→확정→전파` 단계를 〔 〕로 표기, 확정·전파는 조사·분석 게이트 통과가 전제(red-team 트랙과 동일 원칙). 사용자 피드백 반영: "도메인별로도 조사·분석·연구 단계를 거칠 수 있어야" → 보드 구조에 단계 축 내장. [가정]·[검증]·[미정] 규율 태그와는 별개 축.

**보드 v2 교정(같은 세션, 사용자 교정):** v1 도메인(M 모델조사·X design정합·G 문서·B 비즈니스)은 **작업 프로세스 기준이라는 오류** — 사용자 지적: "코어 도메인은 우리 아이디어와 PRD가 낳은 도메인들을 말해야 하고, 그것들의 조사·현황·스펙 차이를 확인할 수 있어야". **v2 = PRD 제품 컴포넌트 기준 8도메인**: SIG 신호엔진·ARC 아키타입(커널) / MAP 지도·PIC NEXT PICKS·LND 랜딩프레임(표면) / SNP 스냅샷공유·COM 커뮤니티(해자) / REV 수익. 각 도메인 = idea 스펙 ↔ design 현황 대조면. 태스크 ID(M·X·G·B)는 작업 계열 표식으로 존속. **v2 렌즈의 즉각 성과: SNP·COM의 design v0.7 현황 미확인 공백 발견 → X7(해자 도메인 현황 질의) 신설.** X5·G2는 크로스커팅으로 분리.

**보드 v2.1(같은 세션, 사용자 요청 2건):** ① **UI/UX를 도메인 체계에 편입** — 각 도메인 카드에 UI/UX 표현 칸 추가(idea·UI/UX·design 3면 대조), 표면 공통 규칙(FR-002 정직표기·FR-012 동등레지스터·FR-013 반응형·카피 언어)을 담는 **UXS 도메인 신설** → v0.7 가드 준수 미점검 공백 발견, **X8(가드 준수 검수) 신설**. ARC의 UI/UX 칸 = [미채움](소프트 코어 표시 형식 미설계, X1 합의에서 채움). ② **점진 채움 원칙** — 보드는 완성형 스냅샷이 아니라 진행하며 채우는 원장, 빈 칸=[미채움] 명시=다음 태스크의 씨앗, 한 세션에 도메인 하나씩. "기획이 크다"는 체감에 대한 구조적 응답: v0 빌드 스코프는 랜딩 한 장이며 보드가 커 보이는 건 스펙·정합·검증을 전부 명시화했기 때문. 도메인 9개(커널2·표면3·해자2·시스템1·수익1), 태스크 13+2(X7·X8), 도메인 간 의존 6개.

**UI/UX 경계 규칙(같은 세션, 사용자 질문 "design 레포와 겹치나?"에 대한 확정):** 겹치지 않음 — idea의 UI/UX 칸·UXS = **경험 요구(WHAT: 정보·위계·여정·톤, 전부 FR·clarify 조항 근거)**, design 레포 = **화면 구현(HOW: 레이아웃·비주얼·타이포·모션)**. 리트머스 = "FR·clarify 근거를 못 달면 idea 소관 아님". "design 현황" 칸은 소유가 아니라 관측(버전 스탬프 필수). 접점(X8 검수·ARC [미채움] 합의)은 전부 X 라운드(원장 A) 경유 — 어느 쪽도 상대 산출물 직접 수정 금지. NEXT.md 규약 + 아티팩트 09 경계 섹션에 명문화.

**보드 v2.2(같은 세션, 사용자 지적 3건):** ① **저맥락 도메인 원장 신설** = `ideas/next-save-domains.md` — 세션 히스토리 없이 읽히는 도메인 정의 + 도메인별 "정해야 할 것" 목록. 섹션 단위로 서브에이전트 브리프·design 라운드에 잘라 넣는 용도. CLAUDE.md 문서 표 등록. ② **도메인별 결정 대기 목록 정리** — 원장에 전문, 보드 카드에 "결정" 줄로 요약. ③ **도메인 입도 교정: 블록 단위 → 제품 개념 단위** — "게임을 클릭하면 게임 프로필이 보이는 것"과 "아키타입 커뮤니티"가 ⑤ COMMUNITY 통짜에 뭉개져 있었음(사용자 질문 "왜 안 들어갔어?"의 답 = 블록 입도의 한계). **GME 게임 프로필**(표면 — FR-006 모달 + FR-009 게임 페이지 임베드 + FR-010) 신설, **COM = 아키타입 커뮤니티**("너 같은 사람들"·유저 카운트, 해자)로 재정의. X7 범위 = GME·SNP·COM(⑤ 블록 전체 현황 질의). 도메인 10개, 빈 칸 5개.

**페이즈 목표 설정(같은 세션, 사용자 결정):** **목표 = 스펙 구체화 — v0 빌드 게이트 통과.** 성숙도 낮음/재개봉 도메인의 결정 17건을 확정해 spec 001·PRD에 흡수하면 빌드 착수 가능. 게이트 17건의 해소 경로 = M1(3건)·M2(1)·X라운드(6)·X7관측후(3)·M5(2)·X8(1)·B1(1) — NEXT.md 페이즈 목표 표 + 원장 빌드 게이트 섹션에 명문화. **명시적 유보 4건**(게이트 아님): ARC 명명(임시명 fallback)·COM 매칭 기준·REV 무료/Pro 경계·UXS 위반 처리 절차. **M5 신설**: $0 공유 게이트 설계(PRD §6.5 빌드 前 필수 관문인데 태스크 미존재 상태였음 — 목표 설정이 드러냄). M 계열 정의 확장: "모델 조사" → "idea 내부 조사·설계". 태스크 16개.
